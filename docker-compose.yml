services:
  db:
    image: postgres:18
    restart: always
    container_name: explorer_dev
    environment:
      POSTGRES_DB: explorer_dev
      POSTGRES_USER: developer
      POSTGRES_PASSWORD: dev123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
